# karma Internal Development Handoff

**[🇬🇧 English (current)](./HANDOFF.md) · [🇨🇳 中文](./HANDOFF.zh.md)**

> 📝 **Internal development handoff doc.** A Chinese-primary handoff used by the author and the Claude Code Agents collaborating on karma's development. Records each milestone's known bugs, wrong-diagnosis lessons, and TODOs for the next session.
>
> The Chinese version ([HANDOFF.zh.md](./HANDOFF.zh.md)) holds the full handoff history. This English page is an entry point — for the complete internal context, refer to the Chinese version.

## Quick context for new contributors

If you're a new contributor reading this:

- **User-facing docs** — [README.md](../README.md) / [docs/PRD.md](./PRD.md) / [docs/ARCHITECTURE.md](./ARCHITECTURE.md)
- **Adding a new AI client backend** — [karma/backends/HOWTO.md](../karma/backends/HOWTO.md)
- **Why the current design is what it is** — [docs/RULES_REDESIGN_PROPOSAL.md](./RULES_REDESIGN_PROPOSAL.md) + [docs/REFACTOR_PLAN_RULE_AND_I18N.md](./REFACTOR_PLAN_RULE_AND_I18N.md)
- **Change history** — [CHANGELOG.md](../CHANGELOG.md)

## Recent milestones (latest first)

As of 2026-05-15, karma is at v0.7.2. Recent shipped work:

- ✅ v0.5.0 — `sticky` → `rule` rename across entire codebase, backward-compat `.sticky_id` alias preserved until v0.6.0
- ✅ i18n English-default documentation swap (README / PRD / ARCHITECTURE / SECURITY / CODE_OF_CONDUCT / CLAUDE / HOWTO / .github templates all English primary + `.zh.md` backup)
- ✅ v0.5.1 — `karma rule add` / `karma rule preview` CLI + Claude Code skill template at `skills/karma-rule.md` for natural-language rule input
- ✅ v0.5.2 — engineering-layer i18n: `karma/i18n.py` module with `tr(key, **fmt)` lookup + locale resolution chain + 5 hook injection paths switchable en/zh
- ✅ v0.5.3 + v0.5.4 — i18n full coverage: 28 `suggested_fix` + 28 `CheckHit.trigger` audit-log strings tr()-driven
- ✅ v0.5.5–v0.5.9 — dogfood-driven correctness fixes: testset `python -c` literal exemption, keep_pushing "next push point" planning phrases, locale-agnostic `trigger_key` audit grouping, Bash heredoc description-context exemption (testset local helper → `description_context.py` shared layer)
- ✅ v0.5.10–v0.5.12 — UX polish: `karma --help` lists `rule add/preview` subcommands, `skills/karma-rule.md` clarity audit (5 gaps closed), `karma init` auto-installs skill + new `karma install-skill [--force]` command
- ✅ v0.5.13 — audit-driven dedup: `is_python_c_command` helper extracted to `karma/checks/common.py` (was duplicated across 3 check files), 34 `.sticky_id` callsites cleaned to `.rule_id`, `karma doctor` reports skill installation status
- ✅ v0.5.14 — `karma-rule` skill teaches the modify recipe (`rule preview` → `rule remove && rule add`) via existing commands; no new CLI added, by user principle: don't grow surface area for rare flows
- ✅ v0.5.15 — v0.6.0 preparation; `docs/V0_6_0_PLAN.md` draft + internal 11+4 `from karma.sticky` import migration to `from karma.rule` so v0.6.0 can ship as pure deletion commit
- ✅ v0.5.16 — **`/karma <natural language>` skill actually triggers for the first time**; multi-backend install (Claude Code / Codex / Gemini with Markdown → TOML adaptation); honest disclosure that v0.5.1–v0.5.15 shipped skill at wrong path (`<name>.md` flat instead of required `<name>/SKILL.md` directory) so it never triggered
- ✅ v0.5.17 — README narrative rewrite (skill promoted to top-level section, not patch-style mention); PRD F5 rewritten; ARCHITECTURE + HANDOFF synced to v0.5.16 reality
- ✅ v0.5.18 — `bypass_karma` discriminator refinement (dogfood-found false positive): redirect target must actually be a karma path to count as bypass; symmetric tightening for `has_internal` field-name dimension. Caught while grep'ing `violations.jsonl > /tmp/x` for audit — per rule #7, didn't bypass; root-caused and fixed the regex instead
- ✅ v0.5.19 — `keep_pushing` Agent saturation declaration exemption (dogfood-found): strong saturation phrases (`任务饱和` / `卡在 X` / `明天接力` etc.) exempt the reflection nudge, paired with v0.4.41 user-stop exemption. Caught by the very Stop hook nudging this turn — Agent honestly declaring saturation was being blocked, would have incentivized fake "let me push forward" instead of truthful "I'm saturated." 6-month-old paired-asymmetry gap finally closed
- ✅ v0.5.20 — rule-10 self-audit follow-up: synced ARCH + HANDOFF for v0.5.19 (CHANGELOG had it, technical-archive docs lagged; caught by user-prompted self-audit)
- ✅ **v0.6.0 ⚠️ BREAKING** — Removed `karma.sticky` module, `.sticky_id` @property on `CheckHit`+`Violation`, `karma sticky` CLI subcommand, and `karma.rule`/`karma.cli` aliases (`Sticky` / `MAX_STICKY` / `StickyConfigError` / `EXAMPLE_STICKY*`). Data-compat shims stay (`sticky.yaml`→`rules.yaml` auto-migration, `violations.jsonl` `sticky_id` field fallback). Pure-deletion commit — v0.5.13/15 internal cleanup made it work without refactor. Deprecation cycle: 18 v0.5.x releases. 5 deletion-lock regression tests added (`test_v0600_*`).
- ✅ **v0.6.1 — Issue #1 real-user bug fix** — `record_edit` exempts non-code paths (README / CHANGELOG / docs/ / .gitignore etc.) from pushing `last_edit_ts`. Root cause: `has_recent_test_pass()` returns `last_test_pass_ts >= last_edit_ts`; any edit (even docs) flipped it to False, blocking `git commit` after `docker pytest` pass. Reporter's `_TEST_CMD_RE` fix was wrong layer; real fix is at `record_edit` time-tracking layer. 6 regression tests (4 exemption + 2 dual-control). Maintainer's first real-user dogfood loop: real-test in docker confirmed real bug existed but in a different layer than reported.
- ✅ **v0.7.0 — treat-root-cause refactor: rewrite "真X" defensive prefixes in karma source rule texts**. User caught Agent doing in-context mimicry from karma's own rule injection headers. Reverted attempted `defensive_prefix_stacking` engine check (treat-symptom approach) per user direction. ~140 occurrences rewritten across rule templates + locale + user-facing docs.
- ✅ **v0.7.1 — deep "真X" cleanup follow-up**. User sharpened v0.7.0 critique: synonym substitution (`真→实际/确实`) wasn't enough; defensive modifier itself is unnecessary in most contexts. 10-phase perl pipeline across 100 tracked files: 767 → 120 (84% reduction). 120 remaining all legitimate (named concept 真字狂魔 / eval term 真阳 / engineering dualism 真阻塞 / test fixtures / natural collocations 真心 真话). Doubled artifact bug fixed (`任务任务到饱和`). One batched commit per user "一次性修复完再提交" directive.
- ✅ **v0.7.2 — remove `chinese_plain` Check 3 reactive monitor**. `karma audit` confirmed 0 triggers in 168 violations after v0.7.0+v0.7.1 root-cause cleanup. Check 3 was v0.4.40's reactive treat-symptom hedge (its own code comment said "治症状不治根因"). Same logic user applied to `defensive_prefix_stacking` in v0.7.0 — source treated, symptom monitor obsolete. Removed `_check_repeated_prefix()` + 2 locale keys + 2 dedicated tests. Closes the "treat root not symptom" loop.
- ✅ **v0.7.3 — hand-audit every GitHub-visible doc**. User directive: read each file individually, not batch find/replace; landing pages should read viral-quality not fragmentary. 33 markdowns reviewed, 22 touched. Removed marketing fluff ("≈ 0%" overclaim, "500+ hours tuning"), cleared `sticky` command-name leftovers from v0.6.0, corrected stale numbers (hard-cap 14 → 12, hook count 9 → actual 8), dropped frozen milestone tags (M3 / v0.5.x in titles), relabeled shipped plan docs as archive, rewrote outdated `HOOK_CONFIGURATION_GUIDE.md`. Net −63 lines, 0 test changes (all docs).
- ✅ **v0.7.4 — `keep_pushing` stop-hint regex covers "satisfied / confirmation" category**. Within-turn dogfood: after v0.7.3 ship, user said "感觉已经挺稳定了，不错不错" (satisfied stop signal). Reflection hook still fired because `_USER_STOP_HINT_RE` only covered "tired / dismissive" phrases (`休息吧 / 算了 / 够了`). Per rule #7 treat-root, extended regex with second category: `不错不错 / 挺稳定 / 就这样吧 / 这就行 / 可以了 / OK 了`. 7 new test fixtures including the literal user phrase.
- ✅ **v0.8.0 — i18n signals system: detection phrases externalized, English users fully covered**. User asked the right question: "工程模块全英文就行，反正 LLM 能看懂" — yes for karma's own code, but regex literals match actual user / Agent dialogue, which is whatever language the user speaks. So: 5 detection regexes' phrases moved to `data/signals/<name>/{zh,en}.txt`. New `karma/signals.py` loader does union compile with long-phrase priority. Cross-language character sets don't overlap → no false matches. **Adding a new language = one `.txt` per signal, zero Python code.** 13 new signal unit tests + 4 English coverage tests. `_PUSH_SIGNAL_RE` (Cartesian) deferred to v0.8.1.
- ✅ **v0.8.1 — `push_signals` YAML DSL: Cartesian templates + word vocabularies completes i18n externalization**. User picked option B (templates + vocab DSL) over option A (flat-only). New `.yaml` schema: `templates: ["{subject}\\s+{verb}"]` + `subjects: [...]` + `verbs: [...]` + `phrases: [...]`. `karma/signals.py` adds `load_patterns()` + `_expand_yaml_signals()` with singular-to-plural placeholder resolution and raw-regex preservation. 1106 expanded phrases total. Historical `(?!\s*[吧行])` lookahead lifted into `check()` post-processing as `_PUSHBACK_TAIL_RE` so YAML stays simple. **All 6 detection signals now i18n-externalized.** 6 new signals tests + 2 English push tests = 452/452 passing.
- ✅ **v0.8.2 — code audit pass: dead code purge + sticky/rule naming consistency + bug fix**. User-requested audit. Tools clean but manual grep found 3 dead-code items whose own comments said "v0.6.0 removed" but were never deleted. Broad `sticky` → `rule` cleanup left over from v0.6.0 BREAKING: function names, module-level constant, `doctor` / `audit` / `violations clear` / `rule list` user-facing output, 3 hook stderr outputs. Real bug found: `cmd_violations_clear` was bypassing the v0.5.0+ rule_id/sticky_id compat shim — fixed. Also closed the v0.8.0 oversight of leaving `_COMPLETION_RE` un-externalized — added `completion_words` signal, **7 of 7 signals now i18n-complete**. 455/455 passing.
- ✅ **v0.8.3 — internal refactor: long hook `main()` split + cli.py import dedup** (pure internal, no user-visible change). `stop.py:main` 223→123, `user_prompt_submit.py:main` 159→68, `pre_tool_use.py:main` 128→90 by extracting single-purpose helpers (`_emit_notifications` / `_handle_force_block` / `_handle_keep_pushing_block` / `_advance_turn_state` / `_build_strong_reminder` / `_emit_engine_denial` / `_emit_keyword_denial`). cli.py: 4 function-level duplicate imports removed, `load()` → `load_rules()` standardized. 455/455 passing, 0 behavior change.
- ✅ **v0.8.4 — v0.8.x cumulative doc sync + 1 dead-code v0.8.2 missed**. Stale "6 signals" counts in README / PRD / ARCHITECTURE updated to 7 (post-`completion_words`). `karma/checks/__init__.py:run_checks()` had `sticky_id` param with its own "v0.6.0 removed" comment that was never removed — 4th instance of the same pattern v0.8.2 caught. Removed cleanly, 0 callers affected. 455/455 passing.
- ✅ **v0.8.5 — 3rd code review pass: 2 high-value polish, codebase confirmed clean**. User-requested 3rd round. Tools clean; manual found 2 items: `rule.py:format_for_injection` `tr` import hoisted; `chinese_plain.py` inline `< 30` extracted as `_JARGON_PAREN_MAX_DIST`. Honestly skipped middle/low-value polish (cli.py function-level imports — mock-friendliness; 4 long cli functions — coordinators). Doc audit: test count + signal count + 0 dead links all consistent. v0.8.x series ends in tooling+manual+doc-audit 3-way-confirmed clean state.
- ✅ **v0.8.6 — `agent_saturation` bare-phrase coverage (within-turn dogfood)**. v0.8.5 release notes wording ("真饱和" / "optimization for its own sake") tripped `keep_pushing` reflection because the agent_saturation phrase set had "任务真饱和" but not bare "真饱和", and en.txt had nothing for "diminishing returns" style. Same pattern as v0.7.4 user_stop_hints gap. Added zh + en phrases, 1 new test (6 fixtures). 456/456 passing.
- ✅ **v0.9.0 — injection architecture redesign, 73% per-turn token saving**. User insight after v0.8.6 dogfood: per-turn full re-injection at UserPromptSubmit duplicates what's already in conversation history from SessionStart. Three changes: SessionStart full baseline (covers 4 sources), UserPromptSubmit compact anchor (new `format_anchor_only()`, ~490 tok vs 1817), PostToolUse mid-reinject triggers on session-global byte_seq hitting tightened model threshold (Opus 60K / Sonnet 40K / Haiku 30K). `tool_byte_seq` no longer per-turn reset. 460/460 passing. 1M Opus session: 18.4% → 8.2% (~100K saved, 55% reduction).
- ✅ **v0.9.1 — v0.9.0 doc-sync**. User dogfooded v0.9.0 in fresh session, saw compact anchor format working, requested doc-sync follow-up. Updated PRD F2 + new F2.5 lifecycle table; HOOK_CONFIGURATION_GUIDE per-hook descriptions; session_start.py docstring (had reversed v0.9.0 architecture description). Pure doc patch.
- ✅ **v0.9.2 — issue #2 fix from @fyn1320068837-source (2nd external bug report)**. `test_compact_hooks.py` had 20 hardcoded refs to `/Users/jhz/karma` (maintainer's path) across all 9 tests → passes locally, fails everywhere else including CI. **Verified: GitHub Actions CI had been failing since v0.8.6 (3 releases)** — I shipped "460/460 passing" without ever checking `gh run list`. Same rule-4 violation pattern as v0.6.1 first external dogfood. Fix exactly as reporter suggested: `PROJECT_ROOT` + `PYTHON = sys.executable`. Added to my own checklist: `gh run list` before tag/release.
- ✅ **v0.9.3 — actually fix CI green**. v0.9.2 push: CI still red. Deeper root cause: CI vulture uses `--min-confidence 60`, my local uses `--min-confidence 70`. 60-conf flags 4 truly-dead items (`EXAMPLE_RULES` / `EXAMPLE_RULES_MINIMAL` cli.py aliases; `current_locale` / `reset_cache` i18n.py — all 0 callers) + 1 false positive (`signals.reset_cache` used by tests but vulture only scans `karma/`). Deleted 4, added `whitelist.py` for signals.reset_cache, updated ci.yml. Local-vs-CI gate mismatch was the deep root cause for the v0.8.6→v0.9.2 red streak. Now in checklist: run `vulture --min-confidence 60` (matching CI) locally before tag.
- ✅ **v0.9.4 — 3rd CI root cause: `mypy` strict mode catches `signals.py` `Optional[list]` narrowing**. v0.9.3 push: CI still red, this time on `mypy karma/`. CI runs mypy; my local checklist never did. `_expand_yaml_signals` had `[v for _, v in resolved]` after `any(v is None ...): continue` guard — runtime-safe, mypy can't see it. Fix: `[v for _, v in resolved if v is not None]` narrows to `list[list]`. Added `mypy karma/ && mypy tests/` to local checklist matching CI.
- ✅ **v0.9.5 — 4th CI root cause: tests assume zh locale, CI runs en**. v0.9.4 push: CI still red, this time `pytest` (16 fixture fails). My Mac `LANG=zh_CN.UTF-8` → `is_chinese_user()` True → zh i18n → fixtures `"默契"` / `"偏离"` / `"纯陈述"` pass. CI runner default `en_US.UTF-8` → False → en → fixtures fail. Fix: new `tests/conftest.py:pytest_configure` does `os.environ.setdefault("KARMA_LOCALE", "zh")` before any karma import. Tests now always run zh regardless of host. Added `LANG=en_US.UTF-8 pytest -q` to local checklist (5th gate, catches locale-coupled bugs). **v0.9.2 → v0.9.5: 4 patch releases, each surfaced a different CI root cause my local checklist had been missing.**
- ✅ **v0.9.6 — 5th CI root cause: v0.6.0 BREAKING rename leftover in `verify wheel` step**. v0.9.5 push: CI still red, this time `Verify wheel contains yaml templates` across all 4 matrix jobs. CI verify checks the wheel contains `data/sticky.dev.example.yaml` — but v0.6.0 BREAKING renamed `sticky.*` → `rules.*`. **This step has been failing since v0.6.0 (~9 releases)** — earlier steps (vulture/mypy/pytest) kept failing first and hiding it. Fix: `ci.yml` `expected` list aligned to actual wheel layout (`rules.dev.example.{,zh.}yaml` + `locales/{en,zh}.yaml` + `config.example.yaml` + `skills/karma/SKILL.md`). Added 6th local checklist gate: `python -m build --wheel + verify`. **Local checklist is now a strict superset of CI step order.** Meta-lesson: I was declaring "the root cause" at each layer without verifying CI reached terminal green. The actual deepest layer is the structural mismatch between local checklist and CI pipeline coverage.
- ✅ **v0.9.7 — KARMA_HOME isolation broken in `bypass_karma` check + user-facing sticky residue + regression mechanism**. User question on the v0.9.6 sub-agent audit's "legitimately preserved" list triggered a deeper grep. Found 2 actual bugs the rename sweep missed: (1) `bypass_karma.py:_KARMA_STATE_PATH_RE` hardcoded `\.claude/karma/...` regex — `KARMA_HOME=/tmp/foo` mode bypass attempts completely missed by check; fixed via `_build_state_path_re()` factory from `karma_home()`, plus filename set expanded to catch both `rules.yaml` and `sticky.yaml`. (2) `cli.py:257` hardcoded hint string misleads `KARMA_HOME` users to non-existent path. Plus 5 user-facing residue in `data/locales/zh.yaml` / `data/config.example.yaml` / 2 rules.dev.example zh files / 4 `violations.py` API docstrings. **Regression mechanism**: new `test_no_sticky_in_user_facing.py` locks 7 user-facing files with line-literal whitelist — next time someone introduces old name CI fails. Dev-facing residue (~10 docstrings + test vars) deferred to v0.10.x. 4 new KARMA_HOME isolation tests in `test_bypass_karma.py`. 466/466 both locales.
- ✅ **v0.9.8 — cross-process concurrency race fix + API-enforced atomicity via `update_state(sid, fn)`**. Audit prep for contributor's stress test ("更厉害测试集") surfaced session_state.py's own TODO (line 276-286) admitting need for file lock — never added. Real race scope: multi-process / multi-hook `load → modify → save` second save overwrites first across ALL fields (not just ltp). **Anti-shortcut alignment**: first pass chose contextmanager approach A with "v0.9.8 务实，留 v0.10/v1" framing; user caught it ("咱们要做长期方案，你忘了么？为什么 karma 没制止你走短期路线？") — karma's pure-engineering regex checks can't catch design-intent shortcuts (zero-LLM principle's known limit); human review is the structural backstop. Rolled back + redesigned approach C in alignment: keep `load`/`save` public (tests/ 58 legit lower-level users); add `update_state(sid, fn) -> tuple[state, T]` as production API bundling `_state_lock` (fcntl.flock advisory, Windows no-op); add `read_state(sid)` explicit read-only (atomic `os.replace` makes read-only lock-free). 6 hooks migrated to `update_state`. cli.py 2 read-only sites to `read_state`. 7 new tests including **N=20 subprocess concurrent stress test verifying no lost updates** — real race-fix evidence. 473/473 both locales. **Invariant lives in API shape now, not calling convention** — new hooks can't accidentally skip the lock.
- ✅ **v0.9.9 — onboarding feedback: `karma init` ends with default-rules summary block**. User-driven product direction call after v0.9.8 reliability work: "Can `karma init` give clear feedback at the end — when the Agent helps install karma, the user should be told which default rules are enabled without needing to type any command themselves." Added `_print_default_rules_summary()` helper called at end of `cmd_init`. One line per rule (`id` + first line of `preference`), header bilingual via `init.summary.header` locale key. Agent running `karma init` sees the block on stdout and naturally relays it to user. **Design choice: deliberately no "next steps: run X" tips** — first pass included `karma rule edit / list / remove` tip block, user pushed back ("我不想让用户手动输一条指令"), tips removed. Principle: post-summary modify intents go through Agent ("remove rule X" / "change rule Y" → Agent uses `/karma` skill), not user typing command syntax. 2 new tests including lockdown ensuring future PRs can't reintroduce command tips. 477/477 both locales.
- ✅ **v0.9.10 — onboarding polish: first-paragraph summary + bilingual footer (3% token reassurance + `/karma` in-chat entry)**. v0.9.9 acceptance review surfaced (1) half-sentence truncation: `split("\n")[0]` cut at YAML visual wrap producing "...When facing hard problems" without rest. User picked option (b) first-paragraph (`split("\n\n")[0]`) — each summary entry is now a complete meaning unit, zh ~33 lines / en ~37 lines total. (2) User wanted reassurance footer: "Tested: rule injection accounts for under 3% of per-session token spend; to add or modify rules, type `/karma <natural-language>` in your AI client." Added `init.summary.footer` bilingual locale key, automatically resolves to user's locale via `_resolve_locale()`. `/karma` is in-chat slash command (not shell command) so doesn't violate v0.9.9's "no shell-command tips" rule. New `test_init_summary_footer_matches_user_locale` lockdown ensures Chinese-system users see only Chinese footer and English-system users see only English. 479/479 both locales.
- ✅ **v0.9.11 — observability: `karma audit --by-check` engine-check hit distribution + `/karma` no-arg defaults to this view**. User picked the "check-firing observability" direction with a key design insight: don't invent new entry points — reuse `/karma` (user already knows from v0.9.10 footer). Implementation: (a) `_cmd_audit_by_check()` aggregates by `Violation.trigger_key` (existing v0.5.7 i18n key, format `check.<name>[.<sub>].trigger`) — top-level per-check counts + sub-variant breakdown + dedicated keyword-only bucket; **no schema change** (historical jsonl without trigger_key falls into keyword-only). (b) `--by-check` CLI flag parsed in main dispatch, default audit unchanged. (c) `skills/karma/SKILL.md` "No-argument flow" section: `/karma` empty `$ARGUMENTS` → Agent runs `karma audit --by-check` and relays with brief interpretation, then asks "want to tune?". Real-data validation: author's 187-violation dogfood produces meaningful distribution (`keep_pushing.default` 69%, 86% keyword-only fallback) on first run — design insight confirmed by real data. 2 new tests including backward-compat lockdown. 481/481 both locales. ⚠️ The "86% keyword-only" was later (v0.9.12) found to be an instrumentation bug, not real behavior.
- ✅ **v0.9.12 — data pipeline bug: `_build_strong_reminder` hook fallback dropped `trigger_key` on Violation write**. User's follow-up question to v0.9.11 ("are 1-trigger checks like `bypass_karma` / `evidence.completion` / `testset` redundant or missing real signal?") forced reading raw violations.jsonl — found two records with identical `trigger` text (i18n output of `check.keep_pushing.default.trigger`) where one had `trigger_key` set and one didn't. Root cause: `user_prompt_submit.py:_build_strong_reminder` (v0.4.41 fallback path for user-submits-before-Stop-hook-runs) constructed `Violation` without `trigger_key=h.trigger_key`, while `pre_tool_use.py` / `stop.py` both passed it. So engine hits via this fallback path got recorded with empty trigger_key → v0.9.11's `--by-check` view bucketed them as keyword-only. **Reanalysis**: true `keep_pushing` engine hits ≈ 99 not 20; true `bypass_karma` ≈ 7 not 1; `evidence.completion` ≈ 10; `testset.*` ≈ 5 — none of the "1-trigger checks" were redundant, all under-counted by the bug. Fix: `_build_strong_reminder` now passes `trigger_key`. **Regression lockdown**: new `test_all_hook_violation_writes_pass_trigger_key` statically scans `karma/hooks/*.py` requiring every `Violation(...)` with `rule_id=...` to also have `trigger_key=...`. **No historical jsonl backfill** (rule #5: rewriting old records to make dashboards look better is the "modify past to validate present" pattern this project rejects). Instead `--by-check` view footer prints a caveat about pre-v0.9.12 data. **Meta-lesson**: rule #4 [loud-failure-with-evidence] applies in both directions — claim a result, then verify the result isn't instrument artifact. The user's question was the artifact-revealing prompt. 482/482 both locales.
- ✅ **v0.9.13 — comprehensive instrumentation audit using v0.9.12's pattern as template; 4 correctness bugs caught**. After v0.9.12, user asked "全面排查下，还有没有这种 bug，直接影响 karma 运行准确性和统计准确性的". Launched audit across Type A (field-missing) / B (off-by-one) / C (race) / D (i18n inconsistency). Sub-agent reported 5 findings; per rule #4 each was hand-verified — 1 was sub-agent misjudgment (`agent_id` encoded in filename not payload, design choice not bug). 4 real bugs all fixed: **A1** `load_all()` dropped `agent_id` (asymmetric with `to_json()` write); **B1** turn window cutoff was `cur - window` making `[cur-window, cur]` = N+1 turns not N — worst impact `stop.py:162 force_block` could trigger on already-fixed old violations (config.yaml says "最近 N turn 内" not N+1); **C1** `pre_tool_use.py:98-100` `load + catchup_pending_bg + no save` (I had misjudged as design choice in v0.9.8; sub-agent caught my error). Migrated to v0.9.8's `update_state` architecture. **D1** zh weak_claims had 8 hedge phrases vs en's 23 → Chinese users had ~35% evidence-check recall, expanded to 25 covering all main hedge families. 3 new lockdown tests + 1 existing fixture strengthened with honest comment ("reflects fix correctness, not tweak to pass"). 485/485 both locales. **Meta-pattern confirmed**: v0.9.12 wasn't a one-off — "instrumentation drift between intent and implementation, accumulated through years" is a real category. A single high-quality follow-up question to a confident interpretation can surface a cluster of related peer bugs, not just one.

- ✅ **v0.9.14 — multi-agent cross-audit catches v0.9.13's own regression: `pre_tool_use` `update_state` not wrapped in try/except (fail-closed)**. User asked for another round of "多 Agent 交叉互审". Launched 3 parallel audit agents with viewpoint diversity (8 engine-check logic / config defaults / fail-open contract). Per rule #4 each finding verified — viewpoint 1 mostly design-choice misjudgments (6/8 noise), viewpoint 2 fully clean, **viewpoint 3 caught the real one**: v0.9.13's C1 migration to `update_state` lacked try/except. `update_state` introduces fcntl.flock / save OSError paths the original `load + catchup` didn't have; any exception bubbles → hook returns non-zero → user blocked (fail-closed, opposite of karma design). Fix: wrap in try/except with fallback to bare `load()`. Plus minor: `_LONG_TASK_RE` adds `pip install` (real FN viewpoint 1 found). 2 new regression tests including PreToolUse fail-open lockdown. **Audit SNR comparison**: v0.9.13 single-agent 5 findings / 4 bugs (high SNR — accumulated drift); v0.9.14 3 parallel agents ~9 findings / 2 bugs (1 critical + 1 minor; low SNR — repo clean post-v0.9.13). **Diminishing returns** confirmed: subsequent audits mainly catch the prior round's own regressions. **Rule #4 extended to third direction**: self-verify post-fix (claim a fix, verify fix didn't introduce regression). Multi-agent cross-audit is one practical way. 487/487 both locales.

- ✅ **v0.10.6 — close v0.10.5 deferred 3: emit_context_injection + emit_stop_block backend contracts + model_from_payload hook integration tests**. v0.10.5 audit sweep deferred 3 structural findings because they needed Backend Protocol expansion (larger than single-PR scope). v0.10.6 closes them: (1) Backend Protocol expanded 6 → 8 contract methods — added `emit_context_injection(event_name, additional_context, payload)` + `emit_stop_block(reason, payload)`. JsonHooksBackend default base provides Claude-shape (no behavior change for Claude users); Gemini overrides `emit_stop_block` returning `{}` (AfterAgent has no block concept — fail-open instead of silent-reject). (2) 4 ContextInjection hooks (`session_start.py` / `user_prompt_submit.py` / `post_tool_use.py` / `subagent_start.py`) now route through `protocol_adapter.emit_context_injection` — previously direct-printed Claude `hookSpecificOutput` shape with codex SessionStart/UserPromptSubmit acceptance untested (v0.9.15 same-pattern bit us). (3) Stop hook 2 block paths (`_handle_force_block` + `_handle_keep_pushing_block`) route through `protocol_adapter.emit_stop_block` — previously direct-printed `{decision:"block", reason}` Claude shape, Gemini AfterAgent has no equivalent. Both functions now accept `payload` param + main() passes it through. (4) Added 3 model_from_payload hook integration tests + 2 cross-backend contract tests (3 backend × 2 method = 6 new contract checks). 606/606 passing both locales (was 597, +9 new locks). All 6 gates clean. **6 consecutive v0.10.x releases close the loop**: v0.10.0 architecture split → v0.10.1-3 codex 3 PRs → v0.10.4 karma parity → v0.10.5 audit sweep → v0.10.6 structural close. Backend Protocol now 8-method contract enforced across all backends via `tests/contract/`.

- ✅ **v0.10.5 — 4-perspective cross-audit sweep: 10 findings fixed across docs/functional/state/boundary classes**. User triggered audit after 5 consecutive v0.10.x releases. 3 Claude parallel agents (logic / boundary / docs+tests) + dogfooding-evidence perspective 4 surfaced 18 findings, 17 hand-verified true (94% SNR — rapid iteration accumulates drift faster than v0.9.14 diminishing-returns predicted). v0.10.5 batch-fixed 10: (1) **critical docs** — README FAQ "Codex manual /hooks approval" contradicted main table's auto-trust (v0.10.2 outdated wording, bilingual fix); CODEX_BACKEND.md TODO list 4/5 items already shipped but listed "planned" (split into Completed v0.10.x / Remaining sections bilingual). (2) **functional bug** — post_tool_use.py now consumes canonical `tool_input.write_file_paths` (parallel to `read_file_paths`) so codex `sed -i` writes advance `last_edit_ts`; integration test locked; codex backend follow-up needed (TODO 7) to emit the field. (3) **boundary leak** — protocol_adapter.py removed 2 `codex` literal fallbacks + v0.9.16 re-export; `detect_backend` now solely routes via `sys.argv[0]` `/.codex/` literal; tests mock sys.argv instead of relying on dead fallback. (4) **state / off-by-one** — pre_compact fallback math fix (`999999` cutoff dead path → ts-dimension `recent(24h)`); stop.py adds `catchup_pending_bg` with fail-open fallback (bg pytest finishing right before Stop hook now properly advances last_test_pass_ts); user_prompt_submit strong_reminder writes Violation `turn=current_turn-1` (was attributing prev-turn violations to new turn). (5) **regex / docstring** — chinese_plain `\w` Unicode-aware → explicit ASCII char class (was eating Chinese path segments); model_threshold module docstring updated v0.4.35→v0.9.0+v0.10.4 thresholds; codex._extract_codex_patch_text docstring marks captured vs speculative wrap keys + stderr warning when speculative key fires. (6) **signal wordlist drift** — agent_saturation en.txt added 12 dual entries (was 30% drift matching v0.9.13 D1 threshold); new `test_signals_zh_en_parity_within_30pct` walks all `data/signals/*/{zh,en}.txt` pairs and CI-fails any direction drift > 30% (lockdown so no human audit needed for this class). **3 structural findings deferred to v0.10.6**: F2.2 `emit_context_injection` contract (4 hooks direct-print Claude shape, codex SessionStart shape unverified — v0.9.15 same-pattern bit), F2.3 `emit_stop_block` contract (stop.py decision:block direct-print, codex acceptance unverified), F3.3 3-hook `model_from_payload` integration tests. 597/597 passing both locales (was 595 — +2 new locks). All 6 gates clean. **Meta-pattern reaffirmed**: v0.9.14 diminishing-returns isn't a law — rapid iteration brings drift back; multi-perspective audit value reappears proportional to iteration velocity since last audit.

- ✅ **v0.10.4 — prefer codex payload.model + OpenAI/Codex threshold table (cross-platform attention adaptation)**. User research found karma's mid-turn reinject + model-adaptive thresholds were Claude-only — `gpt-5.5` (1M context) and other GPT-5.x / Codex models all fell to DEFAULT 40K, too tight for 1M-context flagships, disrupting expression. Two karma-maintainer changes: (1) New `karma/model_threshold.py:model_from_payload(payload)` unifies model lookup — payload.model first (Codex official hooks doc says every command hook stdin has model; transcript_path is **explicitly not** a stable hook interface), transcript fallback (Claude protocol-layer limitation for non-SessionStart events). Wired into all 3 hooks (session_start / user_prompt_submit / post_tool_use). Claude behavior unchanged (Claude hooks except SessionStart don't have model field, naturally fall to transcript). Codex behavior upgraded: every codex hook payload carries fresh model slug including post-`/model` switch, karma detects mid-session changes immediately. (2) Added 11 OpenAI/Codex thresholds to `_MODEL_THRESHOLDS`: `gpt-5.5 / gpt-5.4 → 120K`, `gpt-5.3-codex / gpt-5.2-codex / gpt-5.1-codex-max → 80K`, `gpt-5.4-mini / gpt-5.1-codex-mini → 40K`, `gpt-5.4-nano / gpt-5.3-codex-spark / codex-mini → 30K`, `gpt-5 generic → 80K`. Keyword priority preserved (long strings before short, `gpt-5.5` before `gpt-5`, etc.). 15 new tests in test_model_threshold.py. 595/595 passing both locales (was 580). **Honest scope of what v0.10.4 does NOT do**: `PreCompact` not hookable (Codex hook API doesn't expose it; Codex has `enable_request_compression` internal feature flag but not as a lifecycle event). `SubagentStart/Stop` not hookable (Codex has `enable_fanout` / `child_agents_md` under-dev feature flags but no hook events). `PermissionRequest` not integrated (ADR-001 in codex.py — PreToolUse already covers risky-action interception). Mid-turn reinject is the cross-platform substitute. **Meta-pattern**: 4 consecutive v0.10.x releases (v0.10.0 architecture / v0.10.1 shell-as-Read / v0.10.2 SessionStart+exec_command+auto-trust / v0.10.3 pipe reads + cat-3 / v0.10.4 model threshold) shows healthy human-Claude-Codex collaboration cadence. User-driven research → clean task brief → maintainer-side rapid execution. Boundary discipline keeps it sustainable.

- ✅ **v0.10.3 — codex simple pipe reads + user_stop_hints "collaborative waiting" cat-3 + docs wording fix**. Third codex contribution (commit `8c0e136`): `extract_read_paths_from_exec_command()` extended to recognize simple read-only command chains (`head N | tail M` / `cat | head/tail` / `tail | head` variants), constraints: single pipe only, both sides read-only, no xargs/find/recursive (high FP risk). 4 new codex-private tests. karma-side: `user_stop_hints` adds cat-3 "collaborative waiting/pause" (16 zh + 18 en entries: `等候即可` / `不着急赶工` / `先等等` / `just wait` / `no rush` / `take your time` etc.). Real evidence — this 2026-05-16 session accumulated 100+ keep_pushing false-positives because cat-3 was missing. Plus locked known-FN: `"不动 + 等 X"` combo pattern (e.g., `"不 commit 挡 working tree 不动, 等 codex"`) requires combo-pattern engine, single-token wordlist can't safely cover — documented in regression test. Also corrects v0.10.2's wrong wording "codex has no equivalent concepts" → "codex hook API doesn't expose these events" (Codex internally has `enable_request_compression` / `enable_fanout` feature flags but hook API doesn't surface them). 580/580 both locales. **Workflow lesson learned**: reviewed codex's pipe-read PR by `gh pr checkout` but forgot to switch back to main before doing my own keep_pushing wordlist commit — almost committed into codex's PR branch. Caught by git push's "no upstream" warning. Recovered via reset + stash + branch switch without polluting codex's work. Memory note added: review PR → immediately checkout main.

- ✅ **v0.10.2 — second codex-owned PR merged ([#4](https://github.com/jhaizhou-ops/karma/pull/4)): SessionStart event + exec_command→Bash normalization + auto-trust hooks**. Codex closed major gaps to Claude Code coverage. Three concrete additions: (1) Codex 0.130 SessionStart event added to `_HOOK_EVENTS` (was missing) — codex agent now gets full sticky baseline injection at session start; payload captured: `{session_id, transcript_path, cwd, model, source}` fully compatible with Claude shape so `karma/hooks/session_start.py` works without changes. Subtle finding: codex doesn't fire SessionStart at TUI startup, fires before first user prompt — still functionally correct. (2) `_CODEX_TOOL_MAP` adds `exec_command → Bash` mapping + `normalize_tool_input` copies `cmd` (Codex Desktop/rollout shape) into canonical `command` field so `state.record_bash` works on codex; integration test locks `is_test_cmd` recognition + `last_test_pass_ts` advancement. (3) **Bonus** — `CodexBackend.trust_karma_hooks()` mirrors Codex's `trusted_hash` derivation algorithm and writes `[hooks.state]` entries to `~/.codex/config.toml` automatically at install time, eliminating the manual `/hooks` TUI approval step (was karma's worst onboarding friction since v0.10.0). Safety: only generates entries for karma's own wrappers (`is_karma_entry` predicate); third-party hooks (vibe-island etc) never touched. If Codex changes hash algorithm, hooks fall back to `/hooks` "modified" state — no silent-trust drift. karma maintainer counterpart (this commit): README + README.zh codex install table + alert box rewritten from "manual approval required" to "auto-trust takes effect immediately" + bilingual CHANGELOG/HANDOFF/ARCHITECTURE v0.10.2 entries. 575/575 passing both locales (was 568 — +7 codex private tests). All 6 gates + CI 4-job green. **Codex backend coverage now**: SessionStart / UserPromptSubmit / PreToolUse / PostToolUse / Stop all working = 5 of codex 0.130's 6 events (PermissionRequest skipped no karma use case); only PreCompact / SubagentStart / SubagentStop don't apply because Codex's 6 hook events don't expose those lifecycle moments (Codex platform internally has the concepts via `enable_request_compression` stable=true and `enable_fanout` / `child_agents_md` under-development feature flags, but they're not hookable). Earlier "codex has no equivalent concepts" wording was incorrect and corrected post-v0.10.2.. **Meta-pattern**: ownership split validated across 2 consecutive codex PRs — codex velocity high, evidence-backed (real session rollouts), even delivered bonus features beyond explicit ask. v0.10.0 collaboration model now proven.

- ✅ **v0.10.1 — first codex-owned PR merged (shell-as-Read recognition) + karma-side wiring + cross-backend contract tests**. v0.10.0's ownership split paid off immediately: Codex CLI itself opened [PR #3](https://github.com/jhaizhou-ops/karma/pull/3) implementing `CodexBackend.normalize_tool_input()` shell-read detection (tail / head / cat / less / more / wc / file / sed -n / grep / awk single-file patterns, conservative skips for pipe / redirect / wildcards / recursive / `sed -i`), only touching codex-owned files (`karma/backends/codex.py` + new `tests/test_codex_backend.py`) — boundary discipline held perfectly. karma maintainer reviewed, then did the karma-side counterpart explicitly called out in codex's PR description: `karma/hooks/post_tool_use.py` now consumes the canonical `tool_input["read_file_paths"]` list (backend-neutral — any future backend whose normalize_tool_input emits this field automatically benefits, no codex literal in the generic layer). New integration test `test_post_tool_use_records_codex_shell_read_paths` locks the full chain. **End-to-end result**: codex agent's `tail`/`sed` shell reads now register as Read in karma state, subsequent `apply_patch` on same file no longer false-positive denied — closes the last v0.9.16-era codex usability gap. Plus `tests/contract/test_backend_contract.py` adds 14 abstract contract tests × N backends = 42 auto-validations via pytest parametrize; any future Agent platform registered to REGISTRY is automatically validated against the 6-method contract without per-backend boilerplate. CI vulture --min-confidence 60 caught `shlex.shlex.whitespace_split` false-positive on PR #3 — added to `whitelist.py`. 568/568 passing both locales (was 512). **Meta-pattern confirmed**: ownership split is the right answer for cross-platform protocol details — Codex CLI captured 2 real session rollouts as evidence for its PR (better real-world signal than any Claude cross-model audit), and the karma-maintainer counterpart was small + focused because the boundary was clear in advance.

- ✅ **v0.10.0 — backend architecture split: protocol_adapter delegation + 6-method contract + codex ownership handoff**. After v0.9.16 real-codex testing exposed 2 new bugs (Codex rejects `permissionDecision:"allow"` shape — v0.9.15 was wrong; codex shell-as-Read gap because codex has no separate Read tool, reads via `exec_command`+`tail`/`sed`/`cat` which karma's `record_read` doesn't see → `read_first` false-positive denials), user proposed: *"karma 的 hook 和判定的设计可能得针对不同的平台有针对性的开发和维护，你主要负责维护 karma 主程序和 claude 端，codex 端我让 codex 自行开发和测试"*. This is sound — codex protocol details belong to whoever has fastest signal on codex platform changes, which is Codex CLI itself. v0.10.0 formalizes the split: (a) `karma/backends/_base.py:Backend` Protocol now declares 6 contract methods (`pre_install_setup`, `post_install_message`, `normalize_tool_name`, `normalize_tool_input`, `emit_deny`, `emit_allow`) with Claude-shape defaults in `_json_hooks.py`. (b) `karma/backends/protocol_adapter.py` retired to pure dispatch — backend-specific code (`_GEMINI_TOOL_MAP`, `_CODEX_TOOL_MAP`, `parse_apply_patch_envelope`, `_extract_codex_patch_text`, all `normalize_*` envelope-parsing logic) moved into each backend's own file. `detect_backend()` routes by `hook_event_name` (Gemini) or `sys.argv[0]` literal `/.codex/` (codex), returns canonical REGISTRY keys (`claude-code` / `codex` / `gemini-cli`). (c) `karma/checks/read_first.py` removed `_codex_patch_files` field reference — renamed to backend-neutral `multi_file_targets` canonical. (d) **Bug A fixed**: `CodexBackend.emit_allow() → "{}"` per official codex hooks docs ("permissionDecision allow not supported; return `{}` or exit 0"); locked test `test_codex_emit_allow_returns_empty_dict_not_claude_shape` prevents future regressions. (e) **v0.9.17 work integrated**: `CodexBackend.post_install_message()` loud `/hooks` approval reminder + `karma doctor` codex-specific reminder section + README codex alert box (no longer table-row buried). (f) New `docs/CODEX_BACKEND.md` + `.zh.md` defines ownership boundary, 6-method contract, known TODO agenda (shell-as-Read, real hook payload capture, other tool_name mapping, approval state detection) for Codex backend owner. Codex backend now contributable via PRs from Codex sessions — karma maintainer reviews + merges, doesn't dive deep into codex protocol guessing. 512/512 passing both locales (was 510) + all 6 gates + wheel smoke. **Meta-pattern**: when in-house model (Claude) has systematic blind spot guessing another platform's protocol (v0.9.15 + v0.9.16 + v0.10.0 Bug A all exhibit same pattern), the right fix is contributor ownership split not more cross-model audits.

- ✅ **v0.9.16 — codex apply_patch envelope true parser via real captured payload + config DEFAULTS silent-drop + test asserts tightened**. v0.9.15 had explicitly deferred `tool_input` normalization (phase 2 of cross-backend) because real codex envelope shape wasn't yet captured. v0.9.16 closes the gap with **evidence-backed implementation**: parser is locked against a real `custom_tool_call.input` literal captured from a fresh codex 0.130.0 + GPT-5.5 session rollout (`/Users/jhz/.codex/sessions/2026/05/16/rollout-2026-05-16T13-51-47-...jsonl`). Shape discovery: codex passes the entire `*** Begin Patch ... *** End Patch` envelope as a **single string** in `custom_tool_call.input`, not a structured dict; multi-file patches concatenate `*** Update File:` / `*** Add File:` / `*** Delete File:` blocks. Two new functions: `parse_apply_patch_envelope()` returns `[{"op", "path"}, ...]`; `normalize_tool_input()` synthesizes karma canonical `{file_path, new_string, _codex_patch_files}` for codex apply_patch, passthrough for everything else. Wired into both hooks + `read_first.check` iterates `_codex_patch_files` for multi-file coverage (catches the case where only the primary file was Read). `post_tool_use` iterates Update/Add paths → `record_edit` + `record_read` each, so `last_edit_ts` truly advances for multi-file codex commits (closes the v0.9.15-era evidence/commit gate gap). **Investigation footnote**: `codex exec` non-interactive mode does NOT fire user hooks even with `--enable hooks` — verified via `KARMA_DEBUG_DUMP_PAYLOAD` instrumentation + `codex features list`. Captured payload from session rollout instead. Interactive codex (production path) expected to fire hooks normally; defensive `_extract_codex_patch_text()` handles both bare-string and dict-wrap shapes. Plus **Minor #4**: `karma/config.py:load()` iterates `for key in DEFAULTS` so user-config knobs not in DEFAULTS are silently dropped — `reinject_every_n_tokens` was documented as user-tunable but missing from DEFAULTS, fixed by adding it (None → "auto by model" preserved). Plus **Minor #5**: 3 `tests/test_compact_hooks.py` sites with `if "hookSpecificOutput" in output:` conditional branches silently passed if hooks regressed to deprecated shape — tightened to strict `assert output == {}`. 12 new tests (22 total in protocol_adapter) + config test + tightened compact_hooks asserts. **510/510 both locales** (was 498). All 6 local gates pass + wheel smoke test confirms parser works under clean-venv install.

- ✅ **v0.9.15 — cross-model audit (GPT-5.5) catches 3 cross-backend protocol bugs + critical wheel-packaging miss**. User asked for codex GPT-5.5 cross-model audit. Two rounds: high-level + full-repo review. Round 1 caught (1) Gemini BeforeTool needs top-level `{decision, reason}` not Claude's `hookSpecificOutput` shape (Gemini intercepts were no-ops), (2) Gemini tool_name `run_shell_command`/`read_file` not normalized to Claude canonical (zero checks fired on Gemini), (3) Codex `apply_patch` tool_name unhandled (bypassed all edit checks). WebFetch verified all three against Gemini/Codex/Claude Code official hook docs; caught one codex misjudgment (Codex actually accepts new shape). Fixed via new `karma/backends/protocol_adapter.py` (detect_backend + normalize_tool_name + emit_deny/emit_allow). Round 2 full-repo review caught a **separate critical**: `pyproject.toml` force-include never listed `data/signals/`, so pip-installed wheels lacked the entire signal vocabulary tree → `compile_alternation()` returned never-match → all keyword-fallback layers silently dead for every pip-install user (including Claude Code mainstream). Fixed via force-include of whole directory + CI smoke test (build wheel + pip install into clean venv + assert non-empty regex). My 6-gate wheel verify only locked 6 expected files; signals subtree was never in lockdown list (rule #5 lesson extension). User caught me about to ask for fix direction without verifying local config + official docs ("你没有探查就下结论这很不好"). 11 new tests including Gemini-style integration lockdowns. 498/498 passing. **Meta-pattern**: cross-model audit value is real when in-house model has systematic blind spots — Claude's blind spot was "assume Claude's own protocol is universal," GPT-5.5 with different training exposure pulled official refs and flagged this precisely. Phase 2 (apply_patch multi-file parsing) deferred to v0.9.16+.

🔜 Next session — `karma audit` data continues to drive refinement; potential next directions: more language contributions (ja/ko/de/ru), or revisit chinese_plain jargon term list to also be i18n-aware (currently runs only when Chinese ratio is high, but the jargon list inside the check is not externalized — pending real non-dev user request per YAGNI): chinese-plain residual jargon, keep-pushing pattern tuning. The treat-root philosophy from v0.7.x is now the default lens for new check additions.

## Why Chinese is the primary handoff language

The author and the karma project's primary AI collaborator (Claude Code) work in Chinese — the author finds it faster for design reflection. The handoff doc captures those reflections, decision context, and "wrong-diagnosis lessons"; translating each lesson loses nuance.

If you're an English contributor and want to understand a specific historical decision, run any LLM-based translation on the relevant section of [HANDOFF.zh.md](./HANDOFF.zh.md), or open an issue asking the maintainer to translate that section.
